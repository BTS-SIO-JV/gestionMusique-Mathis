<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}M@ Musique{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/4/pulse/bootstrap.min.css">
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{asset('css/app.css')}}">
        <script src="https://kit.fontawesome.com/821e10c733.js" crossorigin="anonymous"></script>
        {% block stylesheets %}{% endblock %}

    </head>
    <body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="{{path('accueil')}}">M@ Musique</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Gestion des Artistes</a>
        <div class="dropdown-menu">
          <a class="dropdown-item" href=" {{path('artistes')}} ">Liste des Artistes</a>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Gestion des albums</a>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="{{path('albums')}}">Listes des albums</a>
        </div>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Admin</a>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="{{path('admin_artistes')}}">Gestion des artistes</a>
          <a class="dropdown-item" href="{{path('admin_styles')}}">Gestion des styles</a>
          <a class="dropdown-item" href="{{path('admin_albums')}}">Gestion des albums</a>
        </div>
      </li>
    </ul>
  </div>




    {# CRASH TEST LOGIN REGISTER EN HAUT A DROITE 
      <div class="">
        <a style="color: red;" href="{{path('app_login')}}">Login</a>
      </div>
      <div class="">
        <a style="color: red;" href="">Register</a>
      </div>#}

<ul class="navbar-nav ms-auto">
  {% if app.user %}
      <li class="nav-item">
        <a class="nav-link" href="#" style="color:white">Bonjour {{ app.user.userIdentifier }}</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
      </li>
  {% else %}
      <li class="nav-item">
        <a class="nav-link" href="{{path('app_login')}}">Login</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{path('app_register')}}">Register</a>
      </li>
  {% endif %}


</ul>


     {# CRASH TEST QUI EST NUL MAIS A GARDER QUAND MEME ! 
     {% if app.user %}
        <div class="login-container">
            <a style="color:#D6F188;">Hello <a style="color:#3FC115;">{{ app.user.userIdentifier }}</a></a>
        </div>
        <div>
          <a href="">0</a>  je sais pas comment faire d'espace entre les deux div 
        </div>
        <div class="login-container">
            <form action="{{ path('app_logout') }}">
              <button>Logout</button>
            </form>
        </div>
    {% else %}
          <div class="login-container">
            <form action="{{path('app_login')}}">
              <button>Login</button>
            </form>
          </div>
          <div class="login-container">
            <form action="{{path('app_register')}}">
              <button>Register</button>
            </form>
          </div>
          
    {% endif %} #}



    {# CRASH TEST LOGIN REGISTER EN HAUT A DROITE #}

</nav>
        {# affiche tous les messages flash #}
        {% for label,messages in app.flashes %}
        <div class="container">
          <div class="alert alert-{{label}} mt-3">
              {% for message in messages %}
                  <p>{{message}}</p>
              {% endfor %}
          </div>
        </div>
        {% endfor %}
        {% block body %}{% endblock %}
        </div>

            <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
            <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>

        {% block javascripts %}
        {% endblock %}
            
    </body>
</html>
